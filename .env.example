# Authentication Configuration
AUTH_ENABLED=false

# Database Configuration  
DATABASE_URL=postgresql://user:password@localhost:5432/fullstack_starter

# JWT Configuration (only used when AUTH_ENABLED=true)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d

# Development Settings
NODE_ENV=development
